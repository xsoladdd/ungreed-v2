fragment LedgerFragment on ledger {
  created_at
  cutoff
  id
  lock
  month
  transactions(
    order_by: { transaction_type: asc }
    where: { is_deleted: { _eq: false } }
  ) {
    ...TransactionFragment
  }
  updated_at
  year
}
