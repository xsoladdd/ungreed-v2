query getLedger($month: Int!, $year: Int!, $email: String!, $cutoff: bpchar!) {
  ledger(
    where: {
      month: { _eq: $month }
      year: { _eq: $year }
      user: { email: { _eq: $email } }
      cutoff: { _eq: $cutoff }
    }
  ) {
    ...LedgerFragment
  }
}
