table:
  name: user_profile
  schema: public
array_relationships:
  - name: users
    using:
      foreign_key_constraint_on:
        column: profile_id
        table:
          name: users
          schema: public
